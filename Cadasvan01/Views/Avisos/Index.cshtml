@model IEnumerable<Cadasvan01.Models.Aviso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mensagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataDoAviso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mensagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataDoAviso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvisoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvisoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvisoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
